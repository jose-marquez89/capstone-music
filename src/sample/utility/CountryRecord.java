package sample.utility;

/**
 * Models report records used on the "Reports" tab
 * of the main user interface.
 *
 * @author Jose Marquez
 */
public class CountryRecord extends ReportRecord {
    public CountryRecord(String name, int amount) {
       super(name, amount);
    }
}
